@{
    ViewData["Title"] = "Import Đơn hàng";
}

<!-- Content Header -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-12">
                <h1 class="m-0">
                    <i class="fas fa-file-upload text-success"></i> Import Đơn hàng từ Excel
                </h1>
            </div>
        </div>
    </div>
</div>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card card-success">
                    <div class="card-header">
                        <h3 class="card-title">Upload File Excel</h3>
                        <div class="card-tools">
                            <a asp-action="Index" class="btn btn-tool text-white">
                                <i class="fas fa-arrow-left"></i> Quay lại
                            </a>
                        </div>
                    </div>
                    
                    <div class="card-body">
                        <div class="alert alert-info">
                            <h5><i class="fas fa-info-circle"></i> Hướng dẫn import</h5>
                            <p><strong>Bước 1:</strong> Download file template Excel mẫu</p>
                            <a asp-action="DownloadTemplate" class="btn btn-success">
                                <i class="fas fa-file-download"></i> Download Template
                            </a>
                            
                            <p class="mt-3"><strong>Bước 2:</strong> Điền thông tin đơn hàng theo format:</p>
                            <ol class="mb-0">
                                <li><strong>Tên khách hàng</strong> (Bắt buộc)</li>
                                <li><strong>Email</strong> (Bắt buộc)</li>
                                <li><strong>Sản phẩm</strong> (Bắt buộc - định dạng: <code>Tên SP:Số lượng; Tên SP 2:Số lượng</code>)</li>
                            </ol>
                            
                            <p class="mt-3 mb-0"><strong>Bước 3:</strong> Upload file đã điền thông tin</p>
                        </div>

                        <form asp-action="Import" method="post" enctype="multipart/form-data">
                            <div class="form-group">
                                <label for="file">
                                    <i class="fas fa-file-excel text-success"></i> Chọn file Excel
                                </label>
                                <div class="input-group">
                                    <div class="custom-file">
                                        <input type="file" name="file" id="file" class="custom-file-input" accept=".xlsx" required>
                                        <label class="custom-file-label" for="file">Chọn file...</label>
                                    </div>
                                </div>
                                @if (!ViewData.ModelState.IsValid && ViewData.ModelState["File"]?.Errors.Count > 0)
                                {
                                    <div class="text-danger small mt-1">@ViewData.ModelState["File"]?.Errors[0].ErrorMessage</div>
                                }
                                <small class="form-text text-muted">
                                    Chỉ chấp nhận file .xlsx (Excel)
                                </small>
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn-success btn-lg">
                                    <i class="fas fa-cloud-upload-alt"></i> Tiến hành Import
                                </button>
                                <a asp-action="Index" class="btn btn-default btn-lg">
                                    <i class="fas fa-times"></i> Hủy
                                </a>
                            </div>
                        </form>
                    </div>
                </div>

                <div class="card card-warning">
                    <div class="card-header">
                        <h3 class="card-title">
                            <i class="fas fa-exclamation-triangle"></i> Lưu ý quan trọng
                        </h3>
                    </div>
                    <div class="card-body">
                        <ul class="mb-0">
                            <li>Tên sản phẩm phải khớp chính xác với tên trong hệ thống</li>
                            <li>Định dạng sản phẩm: <code>Tên SP:Số lượng; Tên SP 2:Số lượng</code></li>
                            <li>Ví dụ: <code>iPhone 15:1; Ốp lưng:2</code> (mua 1 iPhone và 2 ốp lưng)</li>
                            <li>Các dòng có lỗi sẽ bị bỏ qua và báo cáo sau khi import</li>
                            <li>Đơn hàng sẽ được tạo với trạng thái "Chờ hóa đơn"</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        // Update file input label with selected filename
        document.querySelector('.custom-file-input').addEventListener('change', function(e) {
            var fileName = e.target.files[0]?.name || 'Chọn file...';
            var label = document.querySelector('.custom-file-label');
            label.textContent = fileName;
        });
    </script>
}
